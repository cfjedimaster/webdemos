# Hello MD Viewer

This is a **test** file to verify the viewer works.

## Features

- Renders markdown with `marked`
- Toggle between source and rendered view
- Opens `.md` files via file association

```javascript
console.log("Hello from a code block!");
```

> A blockquote for good measure.
